import java.util.Arrays;
import java.util.HashMap; // Note capitalization

// Why is Arrays plural and HashMap singular?
// Because Arrays is a class, not an array data structure, that gives many operations on Arrays,
// but HashMap is a class that IS a hashmap data structure.

public class ArraysHashmapImportNaming {
}
